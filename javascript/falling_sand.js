class Grid {
    initialize(width, height) {
        this.width = width;
        this.height = height;
        this.grid = new Array(width * height).fill(0);
    }
}

class Grid {
    initialize(width, height) {
        this.width = width;
        this.height = height;
        this.grid = new Array(width * height).fill(0)
    }
}

this.grid = new Array(width * height)fill.(0)

this.grid = new Array(width * height).fill(0)

class Grid {
  this.width = width;
  this.height = height;
  this.grid = new Array(width * height).fill(0)
}